// console.log(arr[0]);  // Первый элемент (1)
// console.log(arr[arr.length - 1]);  // Последний элемент (5)

// arr.push(6);        // Добавляет в конец [1, 2, 3, 4, 5, 6]
// arr.unshift(0);     // Добавляет в начало [0, 1, 2, 3, 4, 5, 6]

// arr.pop();         // Удаляет последний элемент → [1, 2, 3, 4, 5]
// arr.shift();       // Удаляет первый элемент → [2, 3, 4, 5]

// arr.splice(2, 1);      // Удаляет 1 элемент с индекса 2 → [1, 2, 4, 5]
// arr.splice(2, 0, 99);  // Вставляет 99 на индекс 2 → [1, 2, 99, 4, 5]
// arr.splice(1, 2, 7, 8); // Удаляет 2 элемента с индекса 1 и вставляет 7, 8 → [1, 7, 8, 4, 5]

// let newArr = arr.slice(1, 3);  // Копирует с индекса 1 по 2 (не включая 3) → [2, 3]

// let arr1 = [1, 2];
// let arr2 = [3, 4];
// let combined = arr1.concat(arr2);  // [1, 2, 3, 4]
// concat - объединяет масиви


///////////////////////////////////////////////////////////////////////////////
                                                                             //
 / / / / / //🔄 Перебор массива (for, forEach, map, filter, reduce)/ / / / / / 
                                                                            //
//////////////////////////////////////////////////////////////////////////////

// for (let i = 0; i < arr.length; i++) {
//     console.log(arr[i]);
// }

// forEach() – выполнить действие для каждого элемента
// arr.forEach(num => console.log(num));

// map() – создать новый массив с изменёнными элементами
// let doubled = arr.map(num => num * 2);  // Умножает каждый элемент на 2

// filter() – создать новый массив с отфильтрованными элементами
// // let evenNumbers = arr.filter(num => num % 2 === 0); Оставит только чётные числа

// reduce() – сложить все элементы в одно значение
// let sum = arr.reduce((acc, num) => acc + num, 0);  // Сумма всех элементов


///////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                ///
/ / / / / / / /📌 Поиск элементов (find, findIndex, includes, indexOf, lastIndexOf) / / / / / / / 
                                                                                                //  
//////////////////////////////////////////////////////////////////////////////////////////////////

// Найти первый подходящий элемент
// let found = arr.find(num => num > 2);  // Найдёт первое число >2

// Найти индекс первого подходящего элемента
// let index = arr.findIndex(num => num > 2);  // Индекс первого числа >2

// Проверить, есть ли элемент в массиве
// console.log(arr.includes(3));  // true (если 3 есть в массиве)

// Найти индекс элемента
// console.log(arr.indexOf(3));  // Индекс первого вхождения 3
// console.log(arr.lastIndexOf(3));  // Индекс последнего вхождения 3


///////////////////////////////////////////////////////////////////////////////////////////////////
                                                                                                ///
/ / / / / / / /🔀 Изменение порядка элементов (reverse, sort, join, split)/ / / / / / / / / /   //
                                                                                                //  
//////////////////////////////////////////////////////////////////////////////////////////////////

// Перевернуть массив
// arr.reverse();  // Разворачивает массив задом наперёд

// Отсортировать массив
// arr.sort();  // Сортирует как строки ["1", "11", "2"]
// arr.sort((a, b) => a - b);  // Правильная сортировка чисел

// Объединить массив в строку
// console.log(arr.join(", "));  // "1, 2, 3, 4, 5"

// Разделить строку в массив
// let str = "яблоко, банан, апельсин";
// let fruits = str.split(", ");  // ["яблоко", "банан", "апельсин"]


///////////////////////////////////////////////////////////////////
                                                                ///
 / / / / / / / /🎭 Проверка условий (every, some) / / / / / / / //
                                                               /// 
//////////////////////////////////////////////////////////////////

// Проверить, все ли элементы соответствуют условию
// let allPositive = arr.every(num => num > 0);  // true, если все > 0

// Проверить, есть ли хотя бы один подходящий элемент
// let hasNegative = arr.some(num => num < 0);  // true, если есть < 0


////////////////////////////////////////////////////////////////////////////////////
                                                                                  //  
  / / / / / / / /🔥 Генерация новых массивов (Array.from, Array.fill) / / / / / / / 
                                                                                  //   
////////////////////////////////////////////////////////////////////////////////////

// Создать массив из строки
// let letters = Array.from("hello");  // ["h", "e", "l", "l", "o"]

// Заполнить массив одинаковыми значениями
// let filledArray = new Array(5).fill(0);  // [0, 0, 0, 0, 0]


////////////////////////////////////////////////////////////
                                                          //
/ / / / / / / /📌 Работа с многомерными массивами/  / / /  
                                                          //
////////////////////////////////////////////////////////////


// let matrix = [
//     [1, 2, 3],
//     [4, 5, 6],
//     [7, 8, 9]
// ];

// console.log(matrix[1][2]);  // 6 (второй массив, третий элемент)

///////////////////////////////////////////////////////////////






//////////////////////
                    //
// *{               //
                    //
// }                //
// Ко всему проекту //
                    //
//////////////////////

// user.Login = '?'; ник
// user.pass = '?'; пароль
//delete. user.? удаляет

// a.split("") разделяет словj yf на буквы 

// a.in

// a.reverse() зеркало разво. масив

// a.slice() вырезает 

// a.sort() сортирует как ряд 

// console.log(
//math.floor() к мен. окру.
// )

// console.log(
  //math.sqrt() корень кв.
// )

// console.log(
  // new Date() - время
// )

// true && true - Оба должны бить правы

// true || false зад. прав

//.toUpperCase() - КАПС
//.toLowerCase() - АНТИ-КАПС

////////////////
//            //
// != не равно//
//            //
////////////////

